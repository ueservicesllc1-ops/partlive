import { colors } from './colors';
import { spacing } from './spacing';
import { typography, presets } from './typography';

export { colors, spacing, typography, presets as textPresets };
export const theme = {
  colors,
  spacing,
  typography,
  textPresets: presets,
};
